---
const { personData } = Astro.props;
---

<a href={"/people/" + personData.slug}>
  <article>
    <h2>{personData.name}</h2>
    <img src={personData.photo} alt="" />
    <p>Stilling: {personData.occupation}</p>
    <p>Alder: {personData.age}</p>
  </article>
</a>
